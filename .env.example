LIBSQL_URL=file:local.db
LIBSQL_AUTH_TOKEN=

# Browser API base. Use /api for local Vite proxy.
VITE_API_URL=/api

# Vite dev proxy target. Set this to the SST Lambda Function URL when running Vite locally.
VITE_API_PROXY_TARGET=https://your-api-url.lambda-url.us-east-1.on.aws
